
passwd --status jane						//to know if whether a user is disabled or locked
chage -l jane							//to know if whether a user is disabled or locked
grep ^kali /etc/passwd						//to know if whether a user is disabled or locked
